# PhoenixApps - Example environment variables
# Copy this file to `.env` and fill in your values for local development.
# For production (Vercel/Render), set these environment variables in the platform UI.

# General
NODE_ENV=development
PORT=4000
APP_URL=http://localhost:4000
FRONTEND_URL=http://localhost:5173
VERCEL=0

# Firebase Admin (server-side)
# Option A (recommended for CI / platform): set a stringified service account JSON
# Use: export FIREBASE_SERVICE_ACCOUNT="$(cat serviceAccountKey.json | jq -c .)"
FIREBASE_SERVICE_ACCOUNT=

# Option B (alternative): point to a service account file on disk for local dev
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Alternatively set project id and use default application credentials
FIREBASE_PROJECT_ID=

# Firebase Web config (client-side) - required by the frontend
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=
FIREBASE_MEASUREMENT_ID=

# SMTP (email)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM="Phoenix Apps <noreply@example.com>"

# QuickBooks (optional)
QUICKBOOKS_CLIENT_ID=
QUICKBOOKS_CLIENT_SECRET=
QUICKBOOKS_ENVIRONMENT=sandbox
QUICKBOOKS_REDIRECT_URI=https://your-app.com/api/quickbooks/callback

# Optional app-level secrets
SESSION_SECRET=change_this_to_a_random_string

# Usage notes:
# - For `FIREBASE_SERVICE_ACCOUNT` you can store the entire JSON as a single-line string.
#   Example (bash):
#     export FIREBASE_SERVICE_ACCOUNT="$(cat path/to/serviceAccountKey.json | jq -c .)"
# - On CI/deploy platforms you can add each variable in the environment settings.
# - For Vercel you may prefer to set `GOOGLE_APPLICATION_CREDENTIALS` or use project-level
#   credentials instead of embedding the JSON in an env var.
# - Keep secrets out of source control. Do NOT commit real credentials to the repo.
# Firebase Configuration (Required)
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# JWT Secret (for fallback auth)
JWT_SECRET=your-secret-key-change-this-in-production

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com

# Firebase Admin (Optional - for production token verification)
FIREBASE_PROJECT_ID=your-project-id
# OR use service account JSON
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# API URL (Optional - only if frontend and backend are separate)
# VITE_API_URL=https://your-backend.vercel.app/api
